
<h1>Make a post!</h1>
<%= render 'shared/errors' %>

<%= form_for :post, url: '/posts', :html => {:class => 'new-post-form'} do |f| %>
	<%= f.text_field :title, placeholder: 'Post title' %><br>
	<%= f.text_area :description, placeholder: 'item description' %><br>

  -->	<%= f.submit "Submit" %>
<% end %>